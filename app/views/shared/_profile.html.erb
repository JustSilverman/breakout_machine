<% if current_user %>
  <div class="profile">
    <span class="user-name"><%= "#{current_user.name}" %></span>
    <%= render 'shared/open_votes' %>
    <a class="logout" href="#">(log me out)</a>
  </div>
<% end %>